""" Создать вручную список, содержащий цены на товары (10–20 товаров), например:
[57.8, 46.51, 97, ...]

А. Вывести на экран эти цены через запятую в одну строку, цена должна отображаться в виде <r> руб <kk> коп (например
«5 руб 04 коп»). Подумать, как из цены получить рубли и копейки, как добавить нули, если, например, получилось 7
копеек или 0 копеек (должно быть 07 коп или 00 коп).
В. Вывести цены, отсортированные по возрастанию, новый список не создавать (доказать, что объект списка после
сортировки остался тот же).
С. Создать новый список, содержащий те же цены, но отсортированные по убыванию.
D. Вывести цены пяти самых дорогих товаров. Сможете ли вывести цены этих товаров по
возрастанию, написав минимум кода?"""

prices = [2.5, 23.76, 7.1, 123.5, 55, 500.99, 4, 19, 24.59, 68.35, 5730.50, 2500, 7800.09]
output_string = ''
for price in prices:
    if price - int(price) > 0:
        ruble, penny = (str(price) + '0').split('.')
        output_string += ruble + " руб " + penny[:2] + " коп, "
    else:
        output_string += str(price) + " руб 00 коп, "
print("пункт А:", output_string[:len(output_string)-2])
print("пункт В:", id(prices))
prices.sort()
print('после сортировки', id(prices), prices)
prices2 = sorted(prices, reverse=True)
print('пункт С:', id(prices2), prices2)
print('пункт D:', prices[len(prices)-5:])
